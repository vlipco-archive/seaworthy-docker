#!/bin/bash
set -eo pipefail

echo "-----------------"

error() {
    echo >&2 " !! $* Aborting."
    exit 1
}

group_output() {
    sed -u 's/^/    │ /'
}

# bundle_artifact="/tmp/artifacts/bundle.tgz"
cp -Rf /tmp/src/dist/* /app
cd /app

echo "CDNizing assets"
  find /app
echo

echo "Build complete. Wait for docker to commit the changes."
